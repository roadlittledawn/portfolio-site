---
import ProjectCardReact from './ProjectCard';
import type { Project } from '../lib/types';

export interface Props {
  project: Project;
  className?: string;
}

const { project, className } = Astro.props;
---

<ProjectCardReact project={project} className={className} client:load />
