---
import SkillCardReact from './SkillCard';
import type { Skill } from '../lib/types';

export interface Props {
  skill: Skill;
  variant?: 'grid' | 'list';
  className?: string;
}

const { skill, variant, className } = Astro.props;
---

<SkillCardReact skill={skill} variant={variant} className={className} client:load />
