---
import BaseLayout from '../../layouts/BaseLayout.astro';
import ProjectDetailPageContent from '../../components/pages/ProjectDetailPageContent';

// Disable prerendering to allow dynamic routes without static paths
export const prerender = false;

const { slug } = Astro.params;
const title = `Project | Clinton Langosch`;
const description = "Project details";
---

<BaseLayout title={title} description={description}>
  <ProjectDetailPageContent client:only="react" />
</BaseLayout>
